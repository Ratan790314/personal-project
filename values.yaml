replicaCount: 2

image:
  repository: your-dockerhub-username/node-app
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: devops.local
      paths:
        - path: /
          pathType: Prefix

